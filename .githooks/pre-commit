#!/bin/sh

echo "🔍 Running spotless check..."
./gradlew spotlessCheck --quiet 2>/dev/null

if [ $? -ne 0 ]; then
    echo ""
    echo "❌ Code format violations found."
    echo ""
    echo "Run './gradlew spotlessApply' to fix."
    echo ""
    exit 1
fi
